# `UpsideDown` : A Sample Testing Directory

- ### All samples are stored in `/samples` and in their respective `tests/test#/new_op/` test directories

- ### See `scripts/` to know how tests are conducted
